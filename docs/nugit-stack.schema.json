{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nugit.local/schemas/nugit-stack.schema.json",
  "title": "NugitStack",
  "type": "object",
  "required": ["version", "repo_full_name", "created_by", "prs"],
  "additionalProperties": false,
  "properties": {
    "version": { "type": "integer", "const": 1 },
    "repo_full_name": { "type": "string", "minLength": 1 },
    "created_by": { "type": "string", "minLength": 1 },
    "prs": {
      "type": "array",
      "items": { "$ref": "#/$defs/stackedPr" }
    },
    "resolution_contexts": {
      "type": "array",
      "items": { "$ref": "#/$defs/resolutionContext" }
    },
    "layer": { "$ref": "#/$defs/stackLayer" }
  },
  "$defs": {
    "stackLayer": {
      "type": "object",
      "required": ["position", "stack_size", "below", "above"],
      "additionalProperties": false,
      "properties": {
        "position": { "type": "integer", "minimum": 0 },
        "stack_size": { "type": "integer", "minimum": 1 },
        "below": { "$ref": "#/$defs/layerPointer" },
        "above": {
          "oneOf": [{ "$ref": "#/$defs/stackPrPointer" }, { "type": "null" }]
        },
        "tip": { "$ref": "#/$defs/stackTip" }
      }
    },
    "stackTip": {
      "type": "object",
      "required": ["pr_number", "head_branch"],
      "additionalProperties": false,
      "properties": {
        "pr_number": { "type": "integer", "minimum": 1 },
        "head_branch": { "type": "string" }
      }
    },
    "layerPointer": {
      "oneOf": [{ "$ref": "#/$defs/branchPointer" }, { "$ref": "#/$defs/stackPrPointer" }]
    },
    "branchPointer": {
      "type": "object",
      "required": ["type", "ref"],
      "additionalProperties": false,
      "properties": {
        "type": { "type": "string", "const": "branch" },
        "ref": { "type": "string" }
      }
    },
    "stackPrPointer": {
      "type": "object",
      "required": ["type", "pr_number", "head_branch"],
      "additionalProperties": false,
      "properties": {
        "type": { "type": "string", "const": "stack_pr" },
        "pr_number": { "type": "integer", "minimum": 1 },
        "head_branch": { "type": "string" }
      }
    },
    "stackedPr": {
      "type": "object",
      "required": ["pr_number", "position"],
      "additionalProperties": false,
      "properties": {
        "pr_number": { "type": "integer", "minimum": 1 },
        "position": { "type": "integer", "minimum": 0 },
        "head_branch": { "type": "string" },
        "base_branch": { "type": "string" },
        "head_sha": { "type": "string" },
        "base_sha": { "type": "string" },
        "status": { "type": "string", "enum": ["open", "merged", "closed"] },
        "has_unabsorbed_changes": { "type": "boolean" },
        "author_github_login": { "type": ["string", "null"] },
        "is_fork": { "type": "boolean" },
        "head_repo_full_name": { "type": ["string", "null"] },
        "base_repo_full_name": { "type": ["string", "null"] }
      }
    },
    "resolutionContext": {
      "type": "object",
      "required": ["user_github_login", "resolution_pr_number"],
      "additionalProperties": false,
      "properties": {
        "user_github_login": { "type": "string", "minLength": 1 },
        "resolution_pr_number": { "type": "integer", "minimum": 1 }
      }
    }
  }
}
